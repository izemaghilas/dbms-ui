import React, { ReactElement } from "react";

import "./styles/ActivityIndicator.css";

const ActivityIndicator: React.FC = (): ReactElement=>{
    return(
        <div className="activity-indicator"></div>
    );
}

export default ActivityIndicator;